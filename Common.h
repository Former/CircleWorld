#pragma once 

#include <irrlicht.h>

#include <vector>
#include <tr1/memory>
#include <string>
#include <sstream>
#include <thread>
#include <mutex>
#include <future>
#include <algorithm>
#include <iostream>

#define IN
#define OUT

#define ARRAY_SIZE(a_Array) (sizeof(a_Array)/sizeof(a_Array[0]))
